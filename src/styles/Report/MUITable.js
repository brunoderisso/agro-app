import theme from "../Utils/theme";

export default {
    header: {
        backgroundColor: theme.colors.predizadark,
    },
    rows: {
        '&:nth-of-type(odd)': {
            backgroundColor: "#f3f3f3",
        },
    }
}